all:
  vars:
    ansible_python_interpreter: "/usr/bin/python3"
### Para AAP el inventario no debe tener usuario ni credenciales — se configuran desde AAP
#   ansible_user: ubuntu
#   ansible_ssh_private_key_file: /home/juanma/.ssh/id_rsa

  hosts:
    server-legacy:
      ansible_host: 192.168.122.172
